[package]
name = "cloudtrail-rs-lambda-sqs"
version = "0.1.0"
edition = "2024"

[dependencies]
cloudtrail-rs-core = { path = "../core", features = ["decode-sqs"] }
cloudtrail-rs-aws = { path = "../aws" }
