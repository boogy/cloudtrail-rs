[package]
name = "cloudtrail-rs-lambda-sns"
version = "0.1.0"
edition = "2024"

[dependencies]
cloudtrail-rs-core = { path = "../core", features = ["decode-sns"] }
cloudtrail-rs-aws = { path = "../aws" }
