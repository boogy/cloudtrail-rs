[package]
name = "cloudtrail-rs-lambda-eventbridge"
version = "0.1.0"
edition = "2024"

[dependencies]
cloudtrail-rs-core = { path = "../core", features = ["decode-eventbridge"] }
cloudtrail-rs-aws = { path = "../aws" }
